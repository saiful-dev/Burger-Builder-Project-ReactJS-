import React from 'react';

import './Spinner.css';

const spinner=()=>(
    <div className={"Loader"}>Loading...</div> 
    // loading... is fallback if spinner don't show then it will show

); 
export default spinner;